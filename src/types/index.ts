export type Locale = 'en' | 'uk' | 'de' | 'es' | 'ru'; // currently supported locales

export type DistanceOptions = {
  addSuffix?: boolean;
};

export type DateRange = {
  from: string;
  to: string;
};
